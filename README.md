# test

Are you running tests on the same file using node, bun and deno? Using this collection of runtime agnostic testing utilities provides a consistent, jest-like api. Exports describe, it, expect, an assertion library and an alias for the test runner of the current runtime.